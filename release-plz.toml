# Release automation follows repository-local crate metadata. release-plz
# computes dependency updates and publish order from the workspace graph.
[workspace]
dependencies_update = true
git_release_enable = true
