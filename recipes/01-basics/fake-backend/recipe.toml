id = "fake-backend"
title = "Fake host backend descriptor"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["stream", "host", "fake", "sandbox-descriptor"]
requires = ["stream-host", "codec/lisp"]
